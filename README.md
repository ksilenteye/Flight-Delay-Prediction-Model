# Flight-Delay-Prediction-Model
The primary goal is to classify whether a flight will be delayed (arriving $> 15$ minutes late) or on-time/early based on pre-flight and in-flight features. The project focuses on building a robust, production-ready pipeline that rigorously prevents data leakage
